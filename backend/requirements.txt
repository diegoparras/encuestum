fastapi>=0.110
uvicorn[standard]>=0.29
sqlmodel>=0.0.16
aiosqlite>=0.19
httpx>=0.27
pydantic>=2.6
email-validator>=2.1
bcrypt>=4.1
# >=2.10 y no >=2.8: el rechazo de un `iat` en el futuro (`_validate_iat`) se
# agregó en PyJWT 2.10, y sin él la ventana de 120 s del lanzamiento de
# `mod_encuestum` (`app/mod/launch.py`) se falsea con un `iat` adelantado. Ese
# módulo lo comprueba igual por su cuenta: son las dos cosas, no una.
pyjwt>=2.10
alembic>=1.13
cryptography>=42.0
asyncpg>=0.29
redis>=5.0
python-multipart>=0.0.9
boto3>=1.34
openpyxl>=3.1

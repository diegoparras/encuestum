-r requirements.txt
pytest>=8.0
# Pineado a propósito: un linter sin pinear hace que el CI se rompa solo cuando
# sale una versión con reglas nuevas, en commits que no tienen nada que ver
# (0.16 amplió mucho el set por defecto). Subir de versión es una tarea
# deliberada, con su propia pasada de limpieza.
ruff==0.15.21

# Python
**/.venv/
**/__pycache__/
**/*.pyc
# Node
**/node_modules/
**/.next/
**/out/
**/next-env.d.ts
**/*.tsbuildinfo
# Data / envs
app_data/
*.db
.env
.env*.local
# OS
.DS_Store

# Local dev helpers (rutas absolutas por máquina)
.claude/

# Generado por dev/moodle/up.ps1: la CA local de Caddy, extraída del volumen
# `caddy_data` en cada arranque. No es código fuente, y cambia cada vez que
# se recrea ese volumen (`docker compose down -v`).
dev/moodle/caddy-root.crt
